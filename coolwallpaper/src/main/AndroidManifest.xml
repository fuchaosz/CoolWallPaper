<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.coolwallpaper"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!--在SDCard中写数据-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--在SDCard中创建与删除文件权限-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--显示图片-->
        <activity android:name=".ShowPictureActivity"/>

        <!--图片列表-->
        <activity android:name=".ShowPictureListActivity"/>

        <!--图片详情-->
        <activity android:name=".ShowPictureDetailActivity"/>

        <!--主页-->
        <activity android:name=".activity.HomePageActivity"/>

        <!--主页2-->
        <activity android:name=".activity.HomePageActivity2"/>

        <activity android:name=".ShowPictureListActivity2"/>

        <!--下载图片信息的服务-->
        <service android:name=".service.PictureResultGetServevice">
            <intent-filter>
                <action android:name="action_download_result_service"/>
            </intent-filter>
        </service>

    </application>

</manifest>
